import Button from "../Button"

export default (
    <div class={{ highlight: props.isHighlighted }}>
        <h1 class="title">{props.title}</h1>

        <>
            Fragment
        </>

        <ul $map={{ items: props.links, as: 'link' }}>
            <li $if={link.isVisible}>
                <a href={link.href}>{link.label}</a>
            </li>
        </ul>

        <Button on>
            Click here!
        </Button>
    </div>
)