export component TemplateLiteralWithFunctionCallPatterns {
  someString: string
  someArrayOfStrings: string[]

  return `
    ${someString.toUpperCase()}
    ${someArrayOfStrings.map((s) => s.toUpperCase()).join(',').toLowerCase()}
  `
}
