import Navigation from "../nav/Navigation.afx"

export default props => (
	<header class="p-5">
		<a href={props.urlToHomePage}>
			{props.urlToHomePage}
		</a>

		<Navigation {...props.navigation}/>
	</header>
);
